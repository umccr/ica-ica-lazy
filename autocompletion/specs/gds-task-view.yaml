---
name: gds-task-view

title: View a task of gds

options:
  - name: task-run-id
    summary: |
      Path to gds file
    type: string
  - name: stdout
    summary: |
      Shows the task stdout
  - name: stderr
    summary: |
      Shows the task stderr
  - name: to-stdout
    summary: |
      Print file to stdout
  - name: browser
    summary: |
      Open file in BROWSER env var