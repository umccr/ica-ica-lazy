---
name: ica-make-md5sum

title: Create an .md5sum prefix of a file

options:
  - name: gds-path
    summary: |
      Path to make md5sum from
    type: string
    completion:
      command_string: |-
        gds-ls "${CURRENT_WORD}"